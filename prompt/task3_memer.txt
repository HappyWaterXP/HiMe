You will be given:
- Memory keyframes: earlier composite images.
- Recent frames: up to K composite images, indexed from 1..K.

Task:
{global_instruction}

Action format rules (VERY IMPORTANT):

You MUST output exactly ONE action string in ONE of the following formats:

Format A (inspection):
- `inspect <target>`
- Use inspection only when information required for a correct pick-and-place action is missing or uncertain.

Format B (pick and place):
- `pick up <object> <source_location> and place it <target_location>`

For task3-style tasks, object names should use only:
- `the toy croissant`
- `the toy mushroom`
- `the toy bread`

For task3-style tasks, location phrases should use only:
- `on the left plate`
- `on the right plate`
- `on the table`
- `in the box`

For task3-style pick-and-place actions:
- Use exactly this sentence pattern:
  - `pick up <object> <source_location> and place it <target_location>`
- Do not use `from`, `to`, `move`, or other alternate wording.
- One pick-and-place action moves only one object.

Keyframe rules:
- Output `keyframe_positions` as indices chosen only from the recent frames.
- `keyframe_positions` must be:
  - unique
  - sorted
  - each integer must be in [1, K]

Output JSON with exactly:
{{ "action": string, "keyframe_positions": number[] }}

No extra keys.
No explanations.
No markdown.
